"use client";

import Landing from "@/components/Landing";

/**
 * Kept as a live URL because it has been linked and bookmarked, but `/` is now
 * the canonical home and renders the same content — see the canonical tag in
 * this route's layout.tsx.
 */
export default function WelcomePage() {
  return <Landing />;
}
