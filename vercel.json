{
  "framework": "nextjs",
  "installCommand": "bun install",
  "buildCommand": "cd packages/shared && npx tsc -p tsconfig.json && cd ../../apps/web && npx next build",
  "outputDirectory": "apps/web/.next"
}
